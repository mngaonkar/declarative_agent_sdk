---
name: disk-space
description: Check free disk space on this machine using df.
---

# Disk space

1. Run `df -h` via the `exec_command` tool (or equivalent).
2. Summarize free space on the main volumes for the user.
3. End with `[[decision:done]]`.
